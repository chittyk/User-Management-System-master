
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        .container {
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            max-width: 500px;
            margin: 0 auto;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
            color: #333;
        }

        img {
            display: block;
            width: 150px;
            height: 150px;
            border-radius: 50%;
            margin: 20px auto;
            object-fit: cover;
        }

        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: none;
        }

        td {
            padding: 10px;
            border: none;
        }

        td:first-child {
            font-weight: bold;
            border-bottom: none;
            color: #555;
        }

        .dashboard-link, .logout-link {
            display: inline-block;
            text-align: center;
            width: 100%;
            padding: 10px;
            margin-top: 20px;
            background-color: #007bff;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .logout-link {
            background-color: #dc3545;
        }

        .dashboard-link:hover {
            background-color: #0056b3;
        }

        .logout-link:hover {
            background-color: #c82333;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Hello, <%= admin.name %></h1>
        
        <img src="/userImages/<%= admin.image %>" alt="Profile picture of <%= admin.name %>">
        
        <table>
            
            <tr>
                <td>Name:</td>
                <td><%= admin.name %></td>
            </tr>
            <tr>
                <td>Email:</td>
                <td><%= admin.email %></td>
            </tr>
            <tr>
                <td>Contact:</td>
                <td><%= admin.mobile %></td>
            </tr>

        </table>
        
        <a href="/admin/dashboard" class="dashboard-link">Go to Dashboard</a><br>
        <a href="/admin/logout" class="logout-link">Logout</a>
    </div>

</body>
</html>
